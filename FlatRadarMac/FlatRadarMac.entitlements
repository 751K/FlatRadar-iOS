<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.app-sandbox</key>
	<true/>

	<!-- App Sandbox 默认禁止出站连接。没有这一条登录请求直接失败。 -->
	<key>com.apple.security.network.client</key>
	<true/>

	<!-- ⚠️ 还缺一条 Keychain Sharing，钥匙串因此是坏的（-34018）：

	         <key>keychain-access-groups</key>
	         <array>
	             <string>$(AppIdentifierPrefix)com.j.kong.FlatRadar</string>
	         </array>

	     macOS 的 data protection 钥匙串要求签名带 application-identifier，
	     而只有声明了 keychain-access-groups，automatic signing 才会去签发带它的
	     描述文件。但签发 Mac App Development 描述文件的前提是**这台 Mac 已在
	     开发者账号里注册**，那是账号层面的改动（占一个每年才重置的设备名额），
	     所以没有擅自做。

	     2026-09-09 实测（`--keychain-selftest`，签名 + 沙盒的真二进制）：
	         data protection + 无描述文件  →  写入失败 -34018 errSecMissingEntitlement
	         旧式文件式钥匙串 + 无描述文件  →  增 / 查 / 删 全部成功

	     也就是说 -34018 是 data protection 特有的，不是沙盒本身的限制。
	     退回旧式钥匙串能立刻跑通，但那违反 Apple TN3137，而且**日后再切回
	     data protection 时旧条目全部查不到**——表现为所有人静默登出。
	     所以这里不退，等注册设备。见 docs/MACOS.md 风险 2。 -->
</dict>
</plist>
